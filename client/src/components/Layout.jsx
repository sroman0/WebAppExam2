// Layout.jsx
// Main layout for the app (header, navigation, content)

export default function Layout({ children }) {
  return (
    <div>
      {/* Navigation and layout UI goes here */}
      {children}
    </div>
  );
}
